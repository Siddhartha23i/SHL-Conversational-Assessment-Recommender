# Streamlit Cloud secrets — set these in the Streamlit Cloud dashboard
# (App → Settings → Secrets)
# Do NOT commit real keys to this file.

GROQ_API_KEY = "your_groq_api_key_here"

# Optional: if deploying the FastAPI backend separately, set the URL here.
# Leave blank to use the embedded agent mode (recommended for Streamlit Cloud).
# RECOMMENDER_API_URL = "https://your-api-host.com"
